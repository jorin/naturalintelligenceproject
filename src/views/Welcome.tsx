import ArticleBody from '../components/ArticleBody/ArticleBody';
import MainContent from '../components/MainContent/MainContent';
import SiteTitle from '../components/SiteTitle/SiteTitle';

const Welcome = () => (
  <MainContent>
    <SiteTitle />
    <ArticleBody>
      <p>This is all coming soon, so if you wandered here, that's neat. And there will be more soon. If everything kind of heads in the direction intended.</p>
    </ArticleBody>
  </MainContent>
);

export default Welcome;
